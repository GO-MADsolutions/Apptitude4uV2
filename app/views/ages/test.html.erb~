

  <h1> TAKE TEST ON AGES </h1>
      
     
 <%= render_breadcrumbs %>
<div ng-controller="tester">
<div ng-repeat="a in pro[0].possibilities">
    {{a.answer}}
    </div>
   <div class="row">
       <div class="col-xs-8">
            <div class="btn-toolbar">
           <button class="btn"
                   ng-repeat="x in pro"
                   ng-class="{'btn-info': x.selected!==null, 'btn-danger': x.selected ===null}"> 
                    <span class="glyphicon"
                          ng-class="{'glyphicon-pencil': x.selected!==null, 'glyphicon-question-sign': 					x.selected===null}"></span>
                   
           </button>
           </div>
       </div>
   </div>
   
  <hr>
    <div class="row">
        <h3>Question:</h3>
        <div class="well well-sm">
            <div class="row">
                <div class="col-xs-12">
                    <h4 ng-bind-html="pro[activequestion].question">{{activequestion+1 + ". "+ pro[activequestion].question}}</h4>
                    <div class="row">
                       
                            <div class="col-sm-6">
                              <h4 class="answer" ng-bind-html="pro[activequestion].possibilities[activequestion].answera">{{pro[activequestion].possibilities[activequestion].answera}}</h4>
                            </div>
                        <div class="col-sm-6">
                              <h4 class="answer" ng-bind-html="pro[activequestion].possibilities[activequestion].answerb">{{pro[activequestion].possibilities[activequestion].answerb}}</h4>
                            </div>
                        <div class="col-sm-6">
                              <h4 class="answer" ng-bind-html="pro[activequestion].possibilities[activequestion].answerc">{{pro[activequestion].possibilities[activequestion].answerc}}</h4>
                            </div>
                        <div class="col-sm-6">
                              <h4 class="answer" ng-bind-html="pro[activequestion].possibilities[activequestion].answerd">{{pro[activequestion].possibilities[activequestion].answerd}}</h4>
                            </div>
                        
                    </div>
                </div>
            </div>
            <button class="btn btn-warning" ng-click="questionAnswered">Continue</button
        </div>
    </div> 
 
  
 
</div>

